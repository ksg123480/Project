DLLMAIN 사용하는이유:프로세스가 DLL을 언제접근하는지 확인하기위해서
(확인이 필요하지않은 DLL인 경우 만들필요없다)
LoadLibrary:로드할때 
FreeLibrary:해제할때
DLL장점  
1.DLL코드는 여러 프로그램에서 공유할 수 있다
(RAM파일에 올라가있음모든프로세스들이 연결해서 사용)
2.공유함으로 디스크 절약(메모리)
3.패치도 간단히 한다(DLL파일만 변경)-update-개념

DLL에서함수의 선언부에서 함수를  export로선언시켜야된다(개발자)
(export안할시 외부에서 접근 x)

사용자입장에서는 반드시 import

-암시적 사용법


-명시적사용법
원할떄 DLL을 링크시키고 
원치않을떄 링크에서 해제시킬수있다